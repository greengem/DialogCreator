'use client'

import { Button } from "@radix-ui/themes"

function FlowActions() {
    return (
        <div className="absolute top-5 left-5 right-5 flex gap-5">
            <Button className="btn btn-primary">Add Node</Button>
        </div>
    )
}